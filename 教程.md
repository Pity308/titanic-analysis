# Titanic 数据分析项目教程（第一阶段 + 第二阶段）

> 目标：
>
> 完成你的第一个数据分析项目。
>
> 做完后你会拥有：
>
> - Python 数据分析环境
> - Jupyter Notebook
> - Titanic 数据分析
> - Pandas 基础
> - 数据可视化基础
> - 第一个可展示的数据项目

---

# 项目结构

最终你的项目目录会长这样：

```text
titanic-analysis/
│
├── analysis.ipynb
├── train.csv
└── images/
```

---

# 第一阶段：环境搭建

---

# STEP 1：安装 Anaconda

官网：

https://www.anaconda.com/download

下载：

- Windows 版本
- Python 3.11 或最新稳定版

安装时一路 Next 即可。

---

# STEP 2：安装 VSCode

官网：

https://code.visualstudio.com/

安装时建议勾选：

- Add to PATH
- Open with Code

---

# STEP 3：安装 Git

官网：

https://git-scm.com/download/win

安装时一路 Next 即可。

---

# STEP 4：打开 Anaconda Prompt

Windows 搜索：

```text
Anaconda Prompt
```

打开。

你会看到类似：

```text
(base) C:\Users\你的名字>
```

---

# STEP 5：创建 Python 环境

运行：

```bash
conda create -n titanic python=3.11
```

如果提示：

```text
Proceed ([y]/n)?
```

输入：

```text
y
```

---

# 如果出现 SSL 报错

先运行：

```bash
conda config --set ssl_verify false
```

再重新执行：

```bash
conda create -n titanic python=3.11
```

---

# STEP 6：激活环境

运行：

```bash
conda activate titanic
```

成功后会看到：

```text
(titanic) C:\Users\你的名字>
```

---

# STEP 7：安装项目需要的库

运行：

```bash
pip install pandas matplotlib seaborn notebook jupyter
```

---

# STEP 8：安装 Jupyter Kernel

运行：

```bash
pip install ipykernel
```

---

# STEP 9：创建项目文件夹

回到桌面：

```bash
cd Desktop
```

创建项目目录：

```bash
mkdir titanic-analysis
```

进入项目：

```bash
cd titanic-analysis
```

---

# STEP 10：用 VSCode 打开项目

运行：

```bash
code .
```

注意：

`.` 不能少。

---

# STEP 11：安装 VSCode 插件

打开 VSCode 左侧：

```text
Extensions
```

安装：

- Python
- Jupyter

都安装微软官方版本。

---

# STEP 12：创建 Notebook

创建新文件：

```text
analysis.ipynb
```

---

# STEP 13：选择 Python Kernel

右上角：

```text
Select Kernel
```

选择：

```text
Python Environments
```

然后选择：

```text
titanic
```

---

# 如果看不到 titanic 环境

回到终端运行：

```bash
conda activate titanic
pip install ipykernel
```

然后：

- 重启 VSCode
- 重新选择 Kernel

---

# STEP 14：测试环境

输入：

```python
print("hello titanic")
```

运行后如果输出：

```text
hello titanic
```

说明环境配置成功。

---

# 第一阶段完成

你已经完成：

- Python 环境
- 虚拟环境
- Jupyter
- VSCode
- Notebook
- Kernel 配置

---

# 第二阶段：第一次数据分析

---

# STEP 15：下载 Titanic 数据集

打开：

https://www.kaggle.com/competitions/titanic/data

下载：

```text
train.csv
```

---

# STEP 16：把 CSV 放进项目目录

你的目录现在应该是：

```text
titanic-analysis/
│
├── analysis.ipynb
└── train.csv
```

---

# STEP 17：读取 CSV

打开 Notebook。

输入：

```python
import pandas as pd

df = pd.read_csv("train.csv")

df.head()
```

---

# df.head() 是什么

```python
df.head()
```

表示：

> 显示前 5 行数据。

这是数据分析最常用操作之一。

---

# STEP 18：查看数据结构

输入：

```python
df.info()
```

你会看到：

- 行数
- 列数
- 数据类型
- 缺失值

例如：

```text
Age 714 non-null
```

说明：

Age 列有缺失值。

---

# STEP 19：查看缺失值

输入：

```python
df.isnull().sum()
```

你会看到类似：

```text
Age        177
Cabin      687
Embarked     2
```

说明：

这些列存在缺失数据。

---

# STEP 20：处理 Age 缺失值

输入：

```python
df["Age"] = df["Age"].fillna(df["Age"].median())
```

这里：

```python
median()
```

表示：

> 计算中位数。

```python
fillna()
```

表示：

> 用中位数填补空值。

---

# STEP 21：验证是否成功

再次运行：

```python
df.isnull().sum()
```

现在应该看到：

```text
Age    0
```

说明缺失值处理成功。

---

# STEP 22：第一次数据可视化

导入 seaborn：

```python
import seaborn as sns
```

---

# STEP 23：分析性别与生存率

输入：

```python
sns.barplot(x="Sex", y="Survived", data=df)
```

你会看到：

- 男性生存率
- 女性生存率

观察：

> 女性生存率明显更高。

---

# STEP 24：写下你的分析结论

创建 Markdown：

```markdown
## Observation

女性乘客的生存率明显高于男性。
```

---

# STEP 25：分析客舱等级与生存率

输入：

```python
sns.barplot(x="Pclass", y="Survived", data=df)
```

你会发现：

- 头等舱生存率更高

---

# STEP 26：保存 Notebook

按：

```text
Ctrl + S
```

---

# 第二阶段完成

你已经学会：

- 读取 CSV
- DataFrame 基础
- 缺失值处理
- Pandas 基础
- 数据可视化
- 数据分析观察

---

# 重要思维

不要试图：

> “先学完 Pandas 再做项目”

正确方式是：

```text
遇到问题
→ 搜
→ 解决
→ 继续推进
```

例如：

- 需要处理缺失值 → 学 fillna
- 需要读 CSV → 学 read_csv
- 需要画图 → 学 seaborn

这才是真实工程师的学习方式。

---

# 下一阶段（第三阶段）

下一步会开始：

- GitHub Repo
- Git 初始化
- git add
- git commit
- git push
- README
- 项目展示

完成后：

你将拥有自己的第一个数据分析作品。